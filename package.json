{
  "name": "kontaki",
  "version": "1.0.0",
  "description": "Software POS ERP Empresarial",
  "scripts": {
    "start": "node server.js",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {}
}
